# Apps

Application code lives here.

- `api/`: NestJS REST API, Prisma, PostgreSQL integration.
- `mobile/`: React Native Expo mobile app.

The mobile app is a client only. It must call the backend API and must never connect directly to PostgreSQL.
